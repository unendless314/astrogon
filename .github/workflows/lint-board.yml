name: Lint Board

on:
  pull_request:
    paths:
      - 'kanban/docs/BOARD.md'
      - 'kanban/scripts/lint-board.mjs'
      - 'package.json'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: node kanban/scripts/lint-board.mjs
